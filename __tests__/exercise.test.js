const func = require('./exercise');

describe('Some description', () => {
  test('some test A', () => {
    // expect(func(1, 2)).toBe(3);
  });

  test('some test B', () => {
    // expect(func(1, 2)).toBe(3);
  });

  test('some test C', () => {
    // expect(func(1, 2)).toBe(3);
  });
});
